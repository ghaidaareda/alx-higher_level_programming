#!/usr/bin/python3
def print_last_digit(number):
    last_digits = []
    for num in str(number):
        last_digit = num % 10 
        last_digit = last_digits.append(last_digit)
    return last_digits
